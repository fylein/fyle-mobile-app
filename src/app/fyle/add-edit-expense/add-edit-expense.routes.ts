import { Routes } from '@angular/router';
import { AddEditExpensePage } from './add-edit-expense.page';

export const addEditExpenseRoutes: Routes = [
  {
    path: '',
    component: AddEditExpensePage,
  },
];
