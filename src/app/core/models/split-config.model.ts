export interface SplitConfig {
  category: {
    is_visible: boolean;
    value: FormData;
    is_mandatory: boolean;
  };
  project: {
    is_visible: boolean;
    value: FormData;
    is_mandatory: boolean;
  };
  costCenter: {
    is_visible: boolean;
    value: FormData;
    is_mandatory: boolean;
  };
}
