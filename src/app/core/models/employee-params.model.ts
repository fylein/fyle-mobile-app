export interface EmployeeParams {
  or: string;
  and: string;
  select: string;
  org_id: string;
  roles: string;
  is_enabled: string;
  has_accepted_invite: string;
  id: string;
  order: string;
  limit: number;
  email?: string;
  user_id: string;
}
