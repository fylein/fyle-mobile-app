import { FyAutoFocusDirective } from './fy-auto-focus.directive';

describe('FyAutoFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new FyAutoFocusDirective();
    expect(directive).toBeTruthy();
  });
});
