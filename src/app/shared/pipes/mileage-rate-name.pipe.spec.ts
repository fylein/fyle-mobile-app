import { MileageRateName } from './mileage-rate-name.pipe';

describe('MileageRateNamePipe', () => {
  it('create an instance', () => {
    const pipe = new MileageRateName();
    expect(pipe).toBeTruthy();
  });
});
