import { AdvanceStatePipe } from './advance-state.pipe';

describe('AdvanceStatePipe', () => {
  it('create an instance', () => {
    const pipe = new AdvanceStatePipe();
    expect(pipe).toBeTruthy();
  });
});
